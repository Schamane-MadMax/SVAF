## What does this PR change?

<!-- Short description. Link related issues with "Closes #NN". -->

## Type of change

- [ ] Specification text (`docs/rfcs/`)
- [ ] JSON Schema (`schemas/`)
- [ ] Reference library (`src/svaf/`)
- [ ] Documentation / tooling

## Checklist

- [ ] `pytest tests/` passes locally
- [ ] Schema changes are covered by `tests/test_schemas.py` (positive and
      negative case)
- [ ] Schema changes that affect what real containers must look like come
      with an RFC changelog entry (RFC-0002 section 5)
- [ ] No personal data, internal paths, or credentials in the diff
      (see CONTRIBUTING)
- [ ] `CHANGELOG.md` updated for user-visible changes
